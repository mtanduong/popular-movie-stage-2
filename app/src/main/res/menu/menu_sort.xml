<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/sort_options"
        android:icon="@drawable/ic_sort_icon"
        android:title="Sort Settings"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/sort_title"
                android:title="@string/sort_by"
                android:enabled="false"
                />
            <item
                android:id="@+id/popular_sort_option"
                android:title="@string/most_popular"
                />
            <item
                android:id="@+id/rating_sort_option"
                android:title="@string/top_rated"
                />
            <item
                android:id="@+id/favorite_sort_option"
                android:title="@string/favorites"
                />
        </menu>

    </item>
</menu>